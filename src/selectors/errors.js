export const getErrors = state => {
    return state.statuses.status
};

export const getStatus = state => {
  return state.statuses.status
};

export const getErrorNumber = state => {
  return state.statuses.errorNumber
}