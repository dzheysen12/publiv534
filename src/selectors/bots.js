export const getBotsList = state => {
  return state.bots.bots
};
export const showAddWindow = state => {
    return state.bots.showAddWindow
};

export const getBotForEdit = state => {
    return state.bots.bot_for_edit
};

export const getMessages = state => {
    return state.bots.messages
};

