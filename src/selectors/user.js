export const getUserObject = state => {
    return state.user.user
};

export const settingsUser = state => {
    return state.user.settings
};

export const dataWasLoad = state => {
    return state.user.dataWasLoad
};

